# reuse-mechanism-adjudicator — round 4

## Findings Index
- [P1] stage-d-remains-unbuilt-position-1-holds — position 1 confirmed live 2026-09-03: no `lib_lens_registry.py`, no `--registry` flag, no `resolve(` call, no `data/`/`harvest/` dir anywhere in interflux or this repo; position 2's "already implements... wired in by default" is false in the present tense (§Stage D / §Why This Approach)
- [P2] reuse-seam-would-not-cover-deepen-or-probe-disagreement — even once Stage D lands, its resolve() gate only ever sees SEED/FUSE/STEER-WIDE lens generation; DEEPEN and PROBE-DISAGREEMENT directives re-dispatch an already-existing lens with no spec and no generate-agents.py call, so "every melange round type" overstates coverage on the design's own terms (§Why This Approach L35-36)

## Findings

### stage-d-remains-unbuilt-position-1-holds
- **Severity:** P1
- **Where:** docs/plans/2026-09-02-linsenkasten-registry.md:647-684 (Stage D, Tasks 16-18: `resolve()`, `RESOLVE_MIN_COSINE`) vs. docs/brainstorms/2026-09-01-linsenkasten-gate-forks-brainstorm.md:24-27, :35-36 (only named seam is "the existing combine/contrast seam")
- **What:** Adjudicating the two quoted positions against the live repos rather than the finding text alone. Position 1 ("unbuilt plan prose, risk remains genuinely open") holds; position 2 ("resolve()/RESOLVE_MIN_COSINE already implements live target-embedding match and is wired into every melange round type by default") is false as worded — it restates round-2's original (self-corrected) claim, not round-3's verified one. Fresh verification today: `find ~/projects/Sylveste/interverse/interflux -iname "*lens_registry*"` → no results; `grep -n "registry\|resolve(" ~/projects/Sylveste/interverse/interflux/scripts/generate-agents.py` → no `--registry` flag, no `resolve(` call anywhere in the file; `ls /Users/sma/projects/.worktrees/interlens-linsenkasten/data` → does not exist; no `harvest/` directory in this repo; `packages/mcp/lib/` here holds only `belief-statements.js, quality-evaluation.js, refinement.js, synthesis.js, thinking-modes.js` — no `embed.js` or `store.js`. Consequently `melange-workflow.js:553,1003,1037` calling `generate-agents.py` today passes no `--registry` flag because the flag does not exist yet — those call sites cannot be "wired in by default" to a mechanism that has zero lines of code. This is not a taste call: it is a directly checkable build-state fact, checked twice now (round-3 and this round) with identical results. Position 2 is the artifact repeating an already-refuted claim, not a fresh disagreement.
- **Evidence:** `find ~/projects/Sylveste/interverse/interflux -iname "*lens_registry*"` → empty; `grep -n "registry\|resolve(" ~/projects/Sylveste/interverse/interflux/scripts/generate-agents.py` → no matches beyond unrelated `.resolve()` path calls (L24, L590, L597 — `Path.resolve()`, not the registry function); this repo has no `data/` or `harvest/` top-level directory; `packages/mcp/lib/` file listing above.
- **Suggestion:** n/a — see remediation.
- **Remediation:** Add an explicit named cross-reference from the brainstorm's "Why This Approach" section (L35-36) to the plan's Stage D `resolve()`/`RESOLVE_MIN_COSINE` mechanism (Tasks 16-18) and state the match-step risk as open until those tasks land and their tests pass — this is the third consecutive round (2, 3, 4) reaching the same verdict while the ratified brainstorm text itself still reads unchanged since 2026-09-01, so the fix needs to land in the document, not in another review round.

### reuse-seam-would-not-cover-deepen-or-probe-disagreement
- **Severity:** P2
- **Where:** docs/brainstorms/2026-09-01-linsenkasten-gate-forks-brainstorm.md:35-36 ("the melange fusion seam already calls interlens tools when available, so reuse-before-regenerate plugs into an existing integration point") vs. `~/projects/Sylveste/interverse/interflux/skills/flux-melange-engine/phases/retarget.md:23-27` and `references/directive-vocabulary.md:19-20,55-65`
- **What:** New insight the contradiction exposed, independent of whether Stage D is built. `retarget.md` Step 3 states plainly: "DEEPEN / PROBE-DISAGREEMENT typically reuse an existing adjacent lens (named in the directive) — no design needed." `directive-vocabulary.md:19-20` confirms DEEPEN "spawns an adjacent lens at the exact location" of a hot cluster — an already-existing lens, re-dispatched, not spec'd or generated. Only FUSE and STEER-WIDE (plus the initial SEED phase) go through `generate-agents.py --from-specs`, the one code path Stage D's Task 17 patches. Per `directive-vocabulary.md`'s own selection priority (`retarget.md:24-27`), DEEPEN is priority #2 and PROBE-DISAGREEMENT is priority #1 — routinely the majority of a round's directives, especially once STEER-WIDE's novel-cluster-rate gate starts skipping it (the worked example at `directive-vocabulary.md:79` shows exactly this: STEER-WIDE skipped, its budget reallocated to DEEPEN). So even a fully-landed Stage D would never see, resolve-match, or dedupe-check the lens bodies DEEPEN and PROBE-DISAGREEMENT dispatch — "wired into every melange round type by default" overstates coverage even taking the disputed finding's own premise (Stage D exists) at face value. This sharpens rather than replaces position 1: the reuse-before-regenerate seam has two independent gaps — it doesn't exist yet (position 1), and once it does, it structurally can't see roughly half of a typical round's lens traffic (this finding).
- **Evidence:** `~/projects/Sylveste/interverse/interflux/skills/flux-melange-engine/phases/retarget.md:23` ("DEEPEN / PROBE-DISAGREEMENT typically reuse an existing adjacent lens... no design needed"); `references/directive-vocabulary.md:19-20` (DEEPEN dispatches an adjacent lens, not a spec); `:64-65` (selection priority: PROBE-DISAGREEMENT first, DEEPEN second); `:79` (worked example: STEER-WIDE skipped, budget reallocated to DEEPEN); only `seed.md:19`, `melange-workflow.js:1003` (FUSE), `:1037` (STEER-WIDE) call `generate-agents.py`.
- **Suggestion:** n/a — see remediation.
- **Remediation:** Scope the brainstorm's reuse-before-regenerate coverage claim (L24-27, L35-36) to name explicitly which melange directive types it can ever reach (SEED/FUSE/STEER-WIDE lens generation) and which it structurally cannot (DEEPEN/PROBE-DISAGREEMENT reuse of already-materialized lenses), so a reader does not assume registry dedupe applies to every lens a melange run touches.

## Verdict
Position 1 holds without qualification: Stage D's `resolve()`/`RESOLVE_MIN_COSINE` is unbuilt in both interflux and this repo as of 2026-09-03 (verified independently a third time, same result as round-3), so position 2's present-tense "already implements... wired in by default" is simply false — it is round-2's original, self-corrected claim resurfacing rather than a live disagreement. Not a taste call. The contradiction was still worth one more pass: chasing "wired into every melange round type" against the actual directive vocabulary surfaces a coverage gap in the design itself that survives even after Stage D eventually lands — DEEPEN and PROBE-DISAGREEMENT dispatch already-existing lenses through a path `generate-agents.py` never sees, so roughly half the directive types in a typical round (per the selection priority and the worked example) will never pass through the reuse-match seam no matter how well Stage D is built.
