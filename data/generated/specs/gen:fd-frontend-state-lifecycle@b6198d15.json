{
  "anti_overlap": [
    "Leaves CSS selector correctness, the scene__ purge, layout/position regressions, and accessibility semantics to the markup/CSS/a11y reviewer.",
    "Leaves copy-table verbatim completeness, brainstorm/register coherence, and acceptance-criteria adequacy to the narrative/copy-QA reviewer.",
    "Does not review Stage.tsx's deletion itself beyond confirming no lingering import references — the CSS and markup consequences of that deletion belong to the other agent."
  ],
  "decision_lens": "Does every stateful/async surface in this rewrite (object URLs, IndexedDB loads, roomId derivation, pure-projection inputs) still resolve to the same observable outcome it does today, in every render and timing order the new architecture allows — including renders and IndexedDB writes the plan's own prose never walks through?",
  "focus": "Whether every stateful and asynchronous surface this rewrite touches or introduces — object-URL creation/revocation in page.tsx, the founding rewrite's caption-to-room grouping feeding foundRoom's roomId derivation, photo persistence ordering, and journalView's purity/determinism contract — still resolves to the same observable outcome under every render and timing order the new architecture allows, not just the one the plan's prose walks through.",
  "name": "fd-frontend-state-lifecycle",
  "persona": "Apply the perspective of a senior React/TypeScript engineer who specializes in auditing client-side state machines for stale-closure bugs, effect-cleanup races, and IndexedDB-backed persistence flows, with particular fluency in Blob/Object-URL lifecycle management in single-page apps and in verifying that a 'pure projection' module actually stays pure once its inputs are assembled by imperative, async caller code.",
  "review_areas": [
    "Object-URL lifecycle in page.tsx's new photo-loading effect (Task 7 Step 1): verify it creates exactly one URL per RanchPhotoRow and revokes exactly those on cleanup, check whether photosByRoom/urlForToken are rebuilt with new identities on every render (rather than memoized), which would make them unstable dependencies and cause revoke-then-recreate churn while <Plate> components still hold references to the just-revoked URLs — and check whether anything re-triggers the getRanchPhotos() load after AccretionSheet's extension flow saves new photos mid-session, since nothing in the plan names a refresh trigger for that case.",
    "FoundingBeat's existing capture-phase already tracks its own objectUrls ref and revokes on capture-loop failure, unmount, and success (this exact pattern is visible in the current file). Confirm the rewritten plates/captions phases (replacing the deleted sweep phase) preserve this exact revoke discipline — the plan describes the phase's UI and grouping logic but never restates the URL bookkeeping obligation.",
    "foundRoom's roomId derivation (lib/ranch.ts: roomId = dedupeAnchors(anchors)[0].token, throws if that token's room already exists) under the new caption-grouping build: the plan says 'the existing persistence code needs only the result.newRooms-shaped input replaced by the caption groups' but never specifies the grouping construction — verify whatever construction the implementation lands on groups by trimmed caption in first-appearance order (a Map keyed by trimmed text, built by scanning the photos array once), preserves anchorsFor's existing token-order contract, and defines what happens when two non-contiguous caption groups trim to the same room name.",
    "journalView's determinism contract (Task 2: 'PURE module, no store reads, no Date.now'): verify plateIndex numbering ('1-based across ALL founded plates') and spread ordering don't leak a dependency on the order page.tsx's async photo-loading loop happens to populate photosByRoom/urlForToken, since that construction lives outside the pure function per the plan's own architecture note.",
    "SweepBoard/AccretionSheet coupling: confirm Task 6's deletion of suggestPiles/aHashBits imports and 'SweepBoard import goes away' from FoundingBeat.tsx leaves lib/similarity.ts's exports and SweepBoard's SweepResult/SweepPhoto shape completely untouched for AccretionSheet.tsx, which imports the identical set today — and confirm Task 6 Step 2's three in-place copy fixes in SweepBoard.tsx (hint gating, button relabels, input width) don't touch anything AccretionSheet.tsx depends on structurally.",
    "Determinism-sweep interaction: scripts/check-tending-determinism.ts is claimed to still pass unchanged — verify the plan's relocation of dateline/nowMs construction into a new page.tsx memo (Task 7 Step 1) doesn't change WHEN any Date.now()-adjacent value is read relative to the mechanics the determinism sweep actually exercises."
  ],
  "severity_examples": [
    "P0: If page.tsx's founded-branch photo effect has photosByRoom/urlForToken (freshly built plain objects, not memoized) in its own dependency array alongside URL.createObjectURL calls in the effect body, every unrelated re-render (a resolveOutcome poll, a daypart tick) revokes and recreates every photo's object URL, and every <Plate> on screen briefly or permanently shows a broken-image icon — on the game's now-primary home surface, every session.",
    "P1: If the caption-grouping construction groups by raw (untrimmed or case-sensitive) caption text while the plan's own rule is 'photos sharing an identical caption join ONE room,' two photos captioned 'Kitchen' and 'kitchen ' silently become two separate rooms instead of one, doubling the room count and desyncing the 'plate {n} — as photographed' numbering the Copy Table promises is stable across all founded plates.",
    "P2: If AccretionSheet.tsx is not re-typechecked or re-tested against SweepBoard's changed copy/markup from Task 6 Step 2, the extension flow compiles today but has zero test coverage catching a future desync between the two consumers of the same component — no cross-consumer test exists for SweepBoard in this plan."
  ],
  "success_hints": [
    "Cites specific line numbers or code excerpts from app/page.tsx, lib/ranch.ts, lib/ranchStore.ts, or components/founding/*.tsx to ground each finding, not just the plan's prose paraphrase of them.",
    "Distinguishes a genuine object-URL or effect-race bug introduced by the rewrite from a pattern FoundingBeat.tsx or AccretionSheet.tsx already handles correctly today — the goal is regressions, not a rewrite of working code.",
    "Flags any place the plan asserts a behavior ('preserved by foundRoom', 'existing persistence code needs only...') without specifying the construction step that would actually make the assertion true.",
    "Treats SweepBoard/AccretionSheet as a shared-consumer surface: any Task 6 change gets checked against both call sites, not just FoundingBeat.tsx.",
    "Stays inside state, effects, async timing, and data-plumbing correctness — does not re-litigate CSS, copy strings, or narrative coherence, which other reviewers own."
  ],
  "task_context": "risk-hunt the journal-home slice-1 plan before execution: this is a presentation + retirement slice (zero mechanic changes claimed) — find what breaks. Stress the behavior-preserving relocation claims (porch spread port of app/page.tsx's since-you-were-gone/rail/overlay wiring), the founding rewrite (caption-grouping rooms vs foundRoom/roomId-stability, photo persistence, SweepBoard surviving only for AccretionSheet), the Stage retirement fallout (scene__ CSS purge vs overlay/TimedLines survivors, dangling references, a11y and link regressions), object-URL lifecycle in page.tsx, the max-w-md layout restructure, copy-table completeness against the shipped-surface QA rules (no pipeline vocab, affordance parity, one CTA per action), and anything the plan silently drops from the current page (resolveOutcome lines, deposited beat, TapeDeck, nav, credits). Also check design coherence against docs/brainstorms/2026-08-30-rancher-loop-brainstorm.md (journal-is-home ruling, founding-as-journal, laws untouched: determinism, absence-as-content, no menace, persist-at-mint, silhouette rule) and that the sealed acceptance criteria actually gate what the plan promises. This reviewer owns the state/effects/async-timing/data-plumbing slice of that goal: object-URL lifecycle, the founding rewrite's caption-grouping-to-roomId pipeline, photo persistence ordering, journalView's determinism contract, and the SweepBoard/AccretionSheet shared-consumer surface."
}
