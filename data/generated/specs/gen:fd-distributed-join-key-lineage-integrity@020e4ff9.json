{
  "anti_overlap": [
    "Do NOT analyze the HKDF/HMAC key-derivation or receipt-signature cryptography in `transfer.py` — that is the crypto agent's territory; you may reference `(session_id, artifact_hash)` as a transport KEY (a partitioning/dedup concern) but not as cryptographic salt/key material.",
    "Do NOT evaluate whether the 3 new pytest tests are individually well-constructed, non-vacuous, or free of mocking/assertion gaps — that is the test-oracle agent's territory; you may use the tests' STATED scenarios as a starting point for reasoning about downstream consequences, but your findings should be about production code paths (gateway.py, redact.py, reconcile.py, sinks), not about test-file quality.",
    "Stay on JOIN-KEY SEMANTICS and cross-consumer composition (does session_id mean the same thing everywhere it's read) rather than on the ingest-time landing mechanics of `receive_device_manifest` itself, which was already reviewed for PR-6/PR-7/PR-8 and is not what changed here."
  ],
  "decision_lens": "For every downstream consumer of session_id (reconcile, redact, gateway, any sink-join), ask: does this code path assume session_id COUNTS something (a distinct real-world capture event, a distinct k-anonymity-contributing unit, a distinct row for a composition budget) in a way that silently breaks when the SAME real-world event can now spawn multiple session_ids (device re-authoring) with NO cross-reference between them? Rank P0 if a composition-budget/k-anonymity accounting path can be made to UNDER-count privacy exposure (i.e., treats two device sessions of the same real capture as fully independent evidence when they are not, weakening the k≥5 guarantee in a way §8.7 was built to prevent) or if a lineage/redaction lookup can attribute artifacts to the WRONG session across the two id regimes. Rank P1 if the 'LOW risk, privacy-conservative under-linking' framing in the research doc is not actually verified against gateway.py code (i.e., is asserted, not demonstrated) and a plausible over-counting or mis-keying path exists. P2 for a real but narrow degradation (e.g., double-billing a query's budget, not a leak). P3 for a documentation-only gap.",
  "focus": "Whether session_id's newly-documented dual provenance (device-random nonce vs. node-content-hash) breaks any DOWNSTREAM consumer that treats session_id as a stable, collision-free, content-meaningful join/partition key — across `reconcile.py`'s within-session dedup, the M4 gateway's `session_join`/`_min_k_across_records` composition-budget accounting, redact.py's session-scoped lineage lookups, and any sink (blurred archive, ecology plates, jawndex, bird audio) that joins by session_id — given that the diff's own 'LOW risk' ranking for the M4 budget is an ASSERTION, not a proof, and the diff explicitly declines to add a provenance discriminator field.",
  "name": "fd-distributed-join-key-lineage-integrity",
  "persona": "Apply the perspective of a distributed-systems engineer who specializes in idempotency keys, partition/join-key design, and the failure modes of composite systems that mix deterministic and random identifiers in the same keyspace — someone who has repeatedly seen a system that is individually correct at each hop (ingest lands fine, transfer dedups fine, query gateway queries fine) develop an emergent integrity bug only when those hops are COMPOSED, because each hop's author reasoned locally about 'is my code correct given the key I was handed' rather than globally about 'does the key mean the same thing everywhere it's compared.'",
  "review_areas": [
    "The diff's own risk ranking calls the M4 `session_join` budget 'LOW — opaque equality key, correct regardless of provenance' and asserts the only exposure is 'under-counted linkage (privacy-conservative).' Read `gateway.py`'s `_surfaces_for` and `_min_k_across_records` directly and verify this claim rather than trusting the prose: does `_min_k_across_records` ever aggregate or compare k across MULTIPLE session_ids in a way where treating two device-random ids for the same real capture as unrelated could instead OVER-state effective k for a linkage query (the opposite of the claimed conservative direction) — e.g. if a query joins on some OTHER field (geohash+hour, garment embedding) that DOES correctly link the two device sessions, but the session_join-specific budget line only ever sees one session_id at a time and never notices the embedding-linkage guard (§8.7) should also be firing for what is actually one recurring subject captured twice.",
    "§8.7 composition-budget cross-talk: CLAUDE.md's own invariant states 'a recurring garment embedding across many geohash-hours can re-identify a habitual subject even when every individual query passes' and requires an embedding-linkage guard that counts similarity-cluster size toward k. If a device re-authors a manifest for the same footage (fresh random session_id, per mk-194), does the embedding-linkage guard still correctly cluster the resulting garment embeddings as coming from related/nearby captures, or does the FRESH session_id give the linkage guard a reason to treat them as more independent than they are (i.e., does anything in the linkage-guard's clustering key on session_id in a way that a re-authored session could exploit to dilute its own recurrence signal)?",
    "`redact.py`'s `_session_id` lookups (lines ~831-836, ~1048, ~1795) pull `session_id` from the manifest to scope redaction/lineage operations — confirm these are correctly session_id-value-based (opaque string equality) and never assume a session_id's SHAPE or provenance implies anything about which detector pipeline ran or which privacy class applies; specifically check whether any redaction-completeness check (`already_redacted` at line ~717) could be fooled by two sessions (one device-random, one content-derived) that happen to reference the SAME underlying raw bytes but are tracked as separate redaction-state entries — could a session already redacted under one session_id be treated as 'not yet redacted' under a second session_id for the identical raw, causing wasted-but-safe re-redaction, OR (the P0-worthy direction) could a session_id COLLISION or reuse cause an UNREDACTED session to be marked already-redacted by mistake?",
    "Sink joins (blurred archive, ecology plates, jawndex, bird audio) — CLAUDE.md states every derived sink 'descends from that one id' on the CONTENT-DERIVED path; verify whether any sink-construction code cross-references or joins sinks BY session_id across the device and contributor paths (e.g., could a jawndex record computed from a device session ever be joined against an ecology-plate record from a DIFFERENT device session of the same real capture, under an implicit assumption that matching session_id means matching capture — the inverse risk: does anything assume DIFFERENT session_id means DIFFERENT/unrelated capture in a way that lets a corpus-level query undercount recurrence for k-anonymity purposes)?",
    "Verify the 'Never duplicate (WITHIN a session)' re-grounding is actually complete: `reconcile.py`'s within-session guarantee is keyed by `(session_id, raw_artifact_hash)` — confirm there is no OTHER dedup/uniqueness assumption anywhere in the reconcile/transfer/gateway stack that still implicitly relies on session_id being globally unique-per-raw (the CROSS-session claim the diff explicitly disclaims) rather than merely unique-per-session, e.g. a cache, an index, or a dict keyed by `raw_artifact_hash` alone somewhere that was never audited in this PR's scope."
  ],
  "severity_examples": [
    "P0: if any §8.7 composition-budget or k-anonymity accounting path can be shown (by reading gateway.py, not by trusting the diff's prose) to treat a device re-authored session as CONTRIBUTING FRESH, unlinked k-anonymity headroom for what is actually a recurring real-world subject — i.e., the 'privacy-conservative under-linking' claim is backwards for some query shape — that directly weakens the k≥5-composes-across-sessions invariant CLAUDE.md declares release-blocking-grade (§8.7), and the diff shipped it as a LOW-risk non-finding without demonstrating the claim against the actual gateway code.",
    "P1: `redact.py`'s already-redacted / lineage-scoping lookups are keyed by session_id under an implicit assumption (predating mk-194) that a session_id uniquely and stably identifies 'the redaction state of this raw' — if two sessions can now legitimately reference the same raw (per the diff's own pinned test), and no code queries 'has this RAW (by artifact_hash) been redacted anywhere', only 'has this SESSION been redacted', the redaction-completeness invariant is now scoped more narrowly than a reader of §8.5 would assume, without that narrowing being flagged as a consequence of mk-194.",
    "P2: two independent sessions for the same raw (the diff's own accepted, tested outcome) means downstream sinks will produce TWO independent derived records (two blurred-archive entries, etc.) for what is one real capture event — inflating corpus volume/duplication in aggregate statistics (jawndex counts, coverage dashboards) without any privacy leak; a data-quality/coverage-equity accounting concern (relevant to the coverage-equity invariant) rather than a privacy breach, but worth flagging since 'coverage confidence is a first-class per-cell contract field' per CLAUDE.md and silent double-counting from re-authored device sessions could skew it."
  ],
  "success_hints": [
    "Open `gateway.py` and read `_surfaces_for` and `_min_k_across_records` in full — do not rely on the diff's one-paragraph LOW-risk characterization; trace an actual `session_join` query through the function and construct (on paper) a two-device-sessions-same-capture scenario to see what k value results.",
    "Cross-reference CLAUDE.md's §8.7 embedding-linkage-guard invariant (composes across sessions, similarity-cluster size counts toward k) against whatever code implements it (search for 'linkage' / 'cluster' / 'recurrence' near the gateway or jawndex modules) and check whether session_id provenance affects clustering inputs.",
    "Grep `redact.py` for every `session_id`-keyed lookup/index/cache and ask, for each, 'is this scoped correctly if the SAME raw legitimately appears under two different session_ids' — name the specific structures (dicts, sets, manifest fields) involved.",
    "Search for any code (sinks, corpus writers, jawndex aggregation) that indexes or dedups by session_id ALONE versus by (session_id, artifact_hash) or by artifact_hash alone, and flag any place a cross-session-same-raw scenario was not in scope for THIS diff's tests but is reachable by the exact mechanism the diff just legitimized (two device manifests, same footage, different random ids).",
    "If the LOW-risk / privacy-conservative claim holds up under direct code inspection, say so explicitly and name the specific lines that make it true (e.g. 'k is computed per-record and MINed, never summed, so under-linking can only ever raise effective k, never lower it below the true value') — a verified P3/no-finding here is valuable given this is the diff's own weakest-scrutinized claim."
  ],
  "task_context": "Attack the central claim that the device random-UUID session_id and the node packet-derived session_id are safely INDEPENDENT. Find any code path — transfer keys/HMAC receipts, reconcile, redact lineage, the M4 session_join budget, sink joins, or the (session_id, artifact_hash) transport key — that DOES assume session_id is content-derived or that two sessions sharing a raw are the same session, such that a device-random-id session silently corrupts lineage, mis-verifies a receipt, double-counts/under-counts a §8.7 composition budget, or lets raw duplicate or strand undetected. Verify the re-grounded 'Never duplicate (WITHIN a session)' claim is actually true on BOTH paths and that the 3 new tests genuinely catch what they assert (especially that the f-027 equality-gate tripwire and the two-random-ids-same-raw independence test can't pass vacuously). Confirm the change is truly docs+tests only with zero behavior change. Rank by §8.1 lineage/join-integrity risk."
}
