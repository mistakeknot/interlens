{
  "anti_overlap": [
    "Do NOT analyze production-code consequences of session_id dual-provenance in gateway.py, redact.py, reconcile.py, or transfer.py's crypto layer as PRODUCTION-CORRECTNESS questions — that is the other two agents' territory; you own only whether the 3 NEW TESTS (and the diff's manual discrimination proof) are genuinely non-vacuous evidence for the claims made about that production code.",
    "Do NOT propose or evaluate NEW tests to add — evaluate the EXISTING 3 tests and the described manual proof strictly as they stand in the diff; a finding should be about what they do or don't currently catch, not a wishlist.",
    "Do NOT re-assess whether the 'docs+tests only, zero behavior change' framing is true for the non-test files (that's a straightforward diff-reading fact any agent can confirm in one line, not this agent's specialist contribution) — focus your specialist lens entirely on test-oracle/discrimination quality of the pytest additions."
  ],
  "decision_lens": "For each of the 3 new tests, ask: construct the MINIMAL code change to `receive_device_manifest`/`reconcile.py`/`transfer.py` that would make the independence claim FALSE (re-derivation, cross-session merge, silent overwrite, session_id coercion) and check whether that exact test would fail. Rank P0 if a test can be shown to pass vacuously (assertions are true independent of the claimed behavior, e.g. asserting a value equals itself, or asserting on a field the code path doesn't touch) or if the 'f-027 tripwire' framing is claimed for a test that would NOT actually fail under the f-027 mutation (an equality/re-derivation gate). Rank P1 if a plausible, realistic regression (not the one specific mutation manually tried) would slip past all 3 tests undetected — e.g., a PARTIAL equality gate, a merge-on-collision path, or a session_id normalization/coercion step. P2 for a test that is correct but weaker than its docstring claims (asserts less than advertised). P3 for narration/naming precision only.",
  "focus": "Whether the 3 new tests in `TestSessionIdProvenanceIndependence` and the diff's own 'discrimination-verified' proof genuinely pin the independence claim as a falsifiable, non-vacuous tripwire — versus asserting behavior that would pass even if the underlying independence claim were false, mocking away the exact mechanism being claimed safe, or the 'injected wrong fix, watched it fail' discrimination test being under-scoped relative to what future regressions could plausibly introduce.",
  "name": "fd-test-oracle-tripwire-integrity",
  "persona": "Apply the perspective of a test-integrity/mutation-testing specialist who treats every 'this pins a design decision' test as a claim to be falsified, not trusted — someone who has repeatedly seen a beautifully-narrated test (rich docstring, named after the exact bug class it claims to prevent) turn out to assert something trivially true regardless of the code under test, or seen a single hand-injected 'wrong fix' discrimination proof (run once, in a PR description, not as CI-enforced mutation coverage) create false confidence that ANY future regression in the same class would be caught, when in fact only the ONE specific mutation tried would be.",
  "review_areas": [
    "`test_device_random_session_id_lands_verbatim_never_rederived`: the docstring claims this is 'the mk-194 f-027 tripwire' — trace precisely which future code change would make this test fail. Does it fail ONLY for a full equality/re-derivation gate (the exact mutation described in the diff's 'Proofs' section), or would it ALSO (correctly) fail for narrower regressions like a session_id NORMALIZATION step (e.g. lowercasing, trimming, re-hyphenating) that changes `result.session_id` without literally calling `derive_session_id`? Conversely, could a regression that calls `derive_session_id` internally but only uses its result for LOGGING/METRICS (not for the landed session_id or dir name) slip past this test entirely while still violating the spirit of 'never re-derived'?",
    "`test_two_random_ids_same_raw_are_kept_independent_not_deduped`: uses a HAND-CONSTRUCTED `same_raw = ['sha256:' + 'c'*64]` — this is a well-formed-looking hash string but verify whether `receive_device_manifest` in this test path actually validates/reconciles `source_artifacts` against a real transport (per the mk-lib reconciliation gate) or whether `transport=None`/absent in this test call means the reconciliation gate is SKIPPED — if skipped, the test proves 'two manifests with the same source_artifacts STRING land as two sessions' but does NOT prove anything about actual RAW BYTES, which is a materially weaker claim than the docstring ('the same raw ... land as two independent sessions') asserts. Confirm whether `_device_manifest()` helper wires a transport or leaves reconciliation inert for this test.",
    "The same test's assertions (`res_a.session_id != res_b.session_id`, distinct dirs, both manifests land with their own session_id) are all true by construction the moment `receive_device_manifest` does ANYTHING other than crash or merge — check whether there is a MORE SPECIFIC assertion this test is missing that would actually distinguish 'correctly kept independent, as designed' from 'accidentally kept independent because nothing ever compares source_artifacts across sessions, which is also true today and would remain true even if some FUTURE session-merge feature partially and buggily overlapped state' — i.e., does this test merely observe the absence of a feature (cross-session merge) that doesn't exist, rather than actively probing a boundary?",
    "`test_device_id_is_not_the_packet_hash_derive_session_id_would_produce`: the final assertions are `v4_shape.match(device_random_sid)` (regex match against a HARDCODED literal the test itself authored) and `callable(derive_session_id)` — assess whether either assertion could EVER fail for any code change short of deleting `derive_session_id` or hand-editing the test's own hardcoded UUID string; if both assertions are true independent of any property of `receive_device_manifest`'s actual behavior, this test provides ZERO regression-detection power over the receive path despite its docstring framing ('proving provenance must be reasoned structurally') — assess whether this is intentional documentation-as-test (acceptable, but should be labeled as such, not counted as a behavioral pin) or a gap the diff overstates.",
    "The diff's 'Proofs (discrimination-verified)' section describes a MANUAL, one-time mutation-and-restore exercise (inject equality gate, watch 2 tests fail, revert) — this is not committed as a CI-enforced mutation test. Assess whether the 3 tests, AS THEY EXIST IN THE REPO going forward, would catch a DIFFERENT plausible regression never manually tried: e.g. (a) a partial/soft equality check that only rejects when session_id ALSO happens to look content-derived (a heuristic gate, not a strict equality gate), (b) a change that merges two sessions' manifests when `source_artifacts` collide (a 'helpful' dedup feature someone adds later believing it fixes 'wasted double-storage'), (c) a session_id COERCION/canonicalization step that changes the string without literally re-deriving it. Name which of these would/wouldn't be caught."
  ],
  "severity_examples": [
    "P0: if `_device_manifest(session_id=..., source_artifacts=same_raw)` constructs a manifest that never actually flows through the mk-lib `reconcile_source_artifacts` gate in this test's call configuration (e.g., because `transport` is omitted/None and reconciliation is conditional on transport being provided), then `test_two_random_ids_same_raw_are_kept_independent_not_deduped` tests 'two manifests with an identical STRING in a JSON field land separately' — a claim that was never in doubt and has nothing to do with RAW BYTES or lineage — while its docstring claims to pin 'the same raw ... land as two independent sessions,' which is a materially stronger and UNTESTED claim.",
    "P1: the f-027 tripwire test would correctly fail for the exact equality-gate mutation manually verified in the PR description, but would NOT fail for a more realistic regression: a future engineer adding 'helpful' session_id canonicalization (e.g., re-encoding to ensure lowercase/hyphenation before landing) that happens to preserve the RANDOM value's bytes but routes it through a code path resembling derivation — the test's assertion (`result.session_id == device_random_sid`) would still pass because the mutation is a no-op on THIS specific already-canonical input, giving false confidence the tripwire is broader than it is.",
    "P2: `test_device_id_is_not_the_packet_hash_derive_session_id_would_produce`'s two assertions are true regardless of `receive_device_manifest`'s behavior (they test a hardcoded string against a regex, and a function's callability) — it functions as executable documentation of the SHAPE ambiguity, not as a regression pin on the receive path; this is a legitimate test-suite role but the docstring's framing ('proving provenance must be reasoned structurally') overstates what the assertions mechanically verify, which could mislead a future maintainer into trusting this test to catch a provenance-confusion regression it structurally cannot detect."
  ],
  "success_hints": [
    "For each of the 3 tests, write out the exact assertion list and then, for each assertion, ask 'what is the SIMPLEST code change that flips this from pass to fail' — if the answer is 'none short of deleting the function/literal,' flag it as non-discriminating.",
    "Read `_device_manifest()` (the test helper) to determine definitively whether `transport` is wired for the two-sessions-same-raw test, and if so what fake/stub Transport is used and whether IT actually verifies content hashes or just tracks presence — this determines whether 'same raw' in the test name is backed by real reconciliation or is purely a JSON-field coincidence.",
    "Re-read the diff's 'Proofs' section discrimination exercise and determine EXACTLY which mutation was tried (read the description literally: 'an equality gate requiring sid to be content-derived') versus enumerate 2-3 OTHER plausible regressions in the same risk class (partial gate, merge-on-collision, coercion) and check each against the 3 tests' actual assertions.",
    "Check git/test history or the test file structure for whether these 3 tests would run in the standard `uv run pytest` / CI gate (not skipped, not marked slow/manual) — a tripwire that isn't wired into the release-blocking gate set has materially less value than the diff's 'release-blocking' framing elsewhere in CLAUDE.md implies for §8.5-grade tested properties, and mk-194 explicitly analogizes to that grade.",
    "If all 3 tests hold up as genuinely discriminating for the realistic regression space (not just the one manually-tried mutation), say so explicitly and name which specific assertion catches which specific regression class — a verified P3/no-finding with named coverage is a strong, useful outcome here."
  ],
  "task_context": "Attack the central claim that the device random-UUID session_id and the node packet-derived session_id are safely INDEPENDENT. Find any code path — transfer keys/HMAC receipts, reconcile, redact lineage, the M4 session_join budget, sink joins, or the (session_id, artifact_hash) transport key — that DOES assume session_id is content-derived or that two sessions sharing a raw are the same session, such that a device-random-id session silently corrupts lineage, mis-verifies a receipt, double-counts/under-counts a §8.7 composition budget, or lets raw duplicate or strand undetected. Verify the re-grounded 'Never duplicate (WITHIN a session)' claim is actually true on BOTH paths and that the 3 new tests genuinely catch what they assert (especially that the f-027 equality-gate tripwire and the two-random-ids-same-raw independence test can't pass vacuously). Confirm the change is truly docs+tests only with zero behavior change. Rank by §8.1 lineage/join-integrity risk."
}
