{
  "anti_overlap": [
    "Do not review the node-side seating arithmetic's correctness (offset math, overlap detection, ledger-range guard logic) as a redaction-recall finding in its own right -- that is fd-frame-basis-seating-semantics's focus; only flag it here if the DIVERGENCE is between what the schema promises and what the Python decoder actually implements, not whether the Python logic alone is correct",
    "Do not review Swift actor-concurrency timing, rollover-boundary races, or currentSegmentIndex derivation -- that is fd-swift-device-authoring's focus; treat the Swift-authored VALUE as given and focus only on whether its WIRE REPRESENTATION and cross-language decode are consistent",
    "Do not re-derive or restate the already-documented additive/optional design intent as a finding -- only report where an implementation demonstrably fails to match that stated intent"
  ],
  "decision_lens": "For every field touched by this diff, ask: do manifest.schema.json, ContractKit/Models.swift, and redact.py's decoder agree on (a) required vs optional, (b) nullable vs absent-only, (c) default-on-absence semantics, and (d) validation ordering relative to persistence? Rank P0 if a schema-valid payload can decode to different semantic meanings in Swift vs Python (a genuine contract fork); P1 if a case is under-covered by conformance vectors such that a regression in one language wouldn't be caught; P2 if the KeySchema/canonical-serialization bookkeeping has a gap that would only surface as a CI failure rather than a silent bug; P3 is polish or comment-only inconsistency.",
  "focus": "Whether the two new contract fields (source_artifact_index, capture_frame_basis) have a single, unambiguous, drift-proof meaning across manifest.schema.json, ContractKit's Swift Codable types, and the Python decoder -- and whether the additive/optional/legacy-preserving guarantees the diff repeatedly asserts actually hold at every one of the three implementations independently.",
  "name": "fd-contract-crosslang-parity",
  "persona": "Apply the perspective of an engineer who specializes in cross-language contract and schema-versioning correctness -- someone who has watched a JSON Schema, a Codable struct, and a dict-based Python decoder quietly diverge over successive PRs because each was edited by whoever touched that side last, who checks optionality/nullability/defaulting semantics field-by-field across all three representations rather than trusting a single 'and this is required in both' comment, and who treats every additionalProperties:false and KeySchema list as a contract surface that must be updated in lockstep or the whole point of 'contract-first' silently fails.",
  "review_areas": [
    "source_artifact_index nullability: the schema declares it `type: integer, minimum: 0` with no null arm (absent-only), Swift's custom decoder explicitly throws on present-and-null, and Python's decoder does `int(entry.get('source_artifact_index', 0))` -- confirm Python's `.get(..., 0)` default has IDENTICAL behavior to 'absent means legacy/0' for every case including a JSON `null` value explicitly present (Python's dict.get returns the default only on KEY ABSENCE, not on a None value -- if source_artifact_index is present as JSON null, entry.get returns None, and int(None) raises TypeError, which the existing except clause maps to schema_invalid -- verify this is the actual behavior and matches Swift's throw-on-null, or whether Python's error message/reason_code diverges from Swift's guarantee in a way a conformance vector would catch",
    "capture_frame_basis is 'required' at the record level (source_artifact_index, node_frame_offset, frame_count all required, additionalProperties:false) -- confirm the ONE new invalid-schema conformance vector (missing frame_count) is exercised by BOTH the Python validator AND the Swift ConformanceRunner, and that there isn't an asymmetric case (e.g. an extra unknown property in a basis record) that Python's jsonschema validator rejects but a hand-rolled Swift decoder would silently ignore via Codable's default forgiving-decode behavior (Swift Codable does not enforce additionalProperties:false by default -- check whether CaptureFrameBasisRecord's Codable conformance is synthesized or manual, and if synthesized, whether an extra key in a basis record actually gets rejected on the Swift side the way `additionalProperties: false` promises on the schema side)",
    "The KeySchema declarations (CaptureFaceRegion.keySchema, CaptureFrameBasisRecord.keySchema, SessionManifest.keySchema) versus the schema's actual `required` list and canonical-serialization sorted-keys rule -- confirm the new keys are correctly registered wherever KeySchema is consumed (canonical serialization / hashing) and that a manifest with the new fields still produces the byte-identical canonical output the CLAUDE.md conventions mandate across Python and Swift for any downstream hash/signature that depends on it",
    "The `capture_face_region` schema $comment change ('frame_index = ... counted WITHIN the source artifact identified by source_artifact_index') is a SEMANTIC redefinition of what frame_index means (it silently changes from 'the node-continuous index' pre-mk-74g-era to 'the per-artifact-local index' now) -- verify no other schema, doc, or code path (outside this diff) still assumes frame_index is node-continuous, especially anything in the M3/M4 record vocabulary or gateway that might reference capture_face_region by field name without knowing this redefinition happened",
    "ManifestGateway.swift's working-state snapshot (the JSONSerialization-based buildTree-adjacent path in SessionController) was updated to mirror the discriminator, per its own comment 'keeping this working-state snapshot in lockstep with the finalize tree' -- confirm there is a test (not just a comment claiming intent) that actually proves the two serialization paths (ManifestWriter's CanonicalValue tree and ManifestGateway's JSONSerialization dict) stay in lockstep for this field, since these are two independently-maintained code paths for what should be the same data"
  ],
  "severity_examples": [
    "P0: a manifest with `\"source_artifact_index\": null` on a capture_face_region entry is schema-valid-shaped enough to pass casual review but Python's `int(entry.get('source_artifact_index', 0))` and Swift's explicit decodeNil-throws diverge in whether/how they reject it -- if Python's TypeError-to-schema_invalid mapping doesn't fire the same way as intended, a null could either crash uncontrolled or (worse) be silently coerced, and no existing conformance vector exercises 'present-and-null' for this specific field to catch the divergence",
    "P1: CaptureFrameBasisRecord's Swift Codable conformance is synthesized (not manual, unlike CaptureFaceRegion above it in the same file), so an extra/unknown key in a basis record is silently accepted by Swift's decoder while Python's jsonschema `additionalProperties:false` rejects it at validate() -- the contract's 'additionalProperties:false enforced both languages' invariant (stated as load-bearing elsewhere in this codebase for the untrusted-input boundary) has a gap specifically for this new record type, uncovered by any conformance vector since the existing invalid-schema vector only tests a MISSING field, not an EXTRA one",
    "P2: the frame_index semantic redefinition ($comment change from node-continuous to per-artifact-local) isn't accompanied by a grep-verified sweep of other schema/doc references to capture_face_region.frame_index, so a stale doc or downstream M3/M4 consumer could still describe the old semantics"
  ],
  "success_hints": [
    "Checks actual Codable synthesis (manual init(from:)/encode(to:) vs default synthesized conformance) for CaptureFrameBasisRecord specifically, since CaptureFaceRegion right above it in the same diff got a manual decoder for exactly the null-vs-absent distinction -- ask why the new record type didn't need the same treatment, or whether it does and doesn't have it",
    "Distinguishes schema-level guarantees (additionalProperties:false, required) from what Swift's Codable machinery actually enforces at runtime -- these are not automatically the same and the codebase's own stated invariant is that they must be",
    "Points to the specific existing or MISSING conformance vector that would (or should) catch each divergence, rather than a general 'the languages might diverge' claim",
    "Traces the frame_index redefinition's blast radius by checking sibling schema/doc files, not just the two lines changed in manifest.schema.json"
  ],
  "task_context": "Find any path where the capture_frame_basis translation mis-seats a box or lets a face go unredacted, where a fail-closed raise (missing basis, out-of-span index, past-ledger index) can be bypassed or tricked into silently cross-seating, or where the legacy single-artifact 1:1 path is not byte-for-byte preserved. Verify the Swift<->schema<->node contract for the two new fields can't drift, and that the ledger-range guard actually fires on a drift-C count skew. Rank by S8.5 redaction-recall risk."
}
