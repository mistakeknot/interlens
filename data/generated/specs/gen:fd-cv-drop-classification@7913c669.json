{
  "anti_overlap": [
    "Do not evaluate the §8.1 transfer protocol, receipts, or reconciliation state machine (owned by fd-crypto-transfer-protocol)",
    "Do not evaluate face/plate redaction recall, cascade.py head-fallback geometry, or recall_fixtures.py realism (owned by fd-vision-redaction-recall)",
    "Byte-identity/cross-language concerns are out of scope here (this module is Python-only); flag only if you find Swift-side encampment logic that duplicates this policy incorrectly"
  ],
  "decision_lens": "For every threshold or comparison operator, recompute the actual boundary by hand (what score does what) and check it against the prose claim. For every 'this produces zero records' claim, trace the true code path a dropped frame takes end-to-end through extract.py rather than trusting the gate's own scorecard. Ask what happens when the classifier is absent, errors, or returns an out-of-range score.",
  "focus": "The §8 encampment drop-class fail-closed grammar: whether the classifier seam, DropPolicy threshold logic, extraction-path wiring, and fixture/gate design actually guarantee zero records of any family for a dropped frame, and whether 'uncertainty resolves to drop' is really enforced rather than merely documented.",
  "name": "fd-cv-drop-classification",
  "persona": "Apply the perspective of a computer-vision safety reviewer specializing in fail-closed classifier gating for sensitive-content exclusion (the same discipline as CSAM/self-harm content-moderation gating) — someone fluent in softmax-proxy-classifier pitfalls, off-by-one threshold bugs in binary decision policies, and the gap between 'a fixture proves the logic' and 'a fixture proves the model,' who reads a docstring's claimed invariant and then re-derives the actual boundary condition from the code rather than trusting the prose.",
  "review_areas": [
    "DropPolicy.should_drop threshold logic: `encampment_score_x100 >= self.certain_clear_below_x100` is the entire predicate — confirm this actually implements the documented three-band rule (confident-clear / uncertain / positive-detection) and doesn't silently collapse the 'uncertain OR positive' fail-closed intent into something that happens to work only because drop_at_x100 > certain_clear_below_x100 by convention, with no validation that a future policy instantiation (or config override) couldn't set drop_at_x100 <= certain_clear_below_x100 and invert the safety margin",
    "extract.py wiring of the encampment check: confirm the classifier runs FIRST, before any person/garment/scene processing, on every sampled frame with no bypassable code path (config flag, missing-classifier fallback, exception-swallowing) that would let extraction proceed on a frame the classifier never actually scored",
    "Behavior when `EncampmentClassifier.classify` raises, times out, returns a score outside 0-100, or the ONNX session fails to load (load_encampment_seam / make_session failure modes) — does any of these degrade to 'treat as clear' instead of the required fail-closed 'treat as drop'",
    "The frames_dropped_encampment session-aggregate counter: confirm it is truly a count with no per-drop geohash/hour/frame-index ever attached anywhere (manifest write path, logs, error messages, or the gate's own per-case reporting) — the docstring explicitly warns a located drop record is itself an encampment observation (proxy leakage)",
    "The MobileNetV2/ImageNet proxy vocabulary (_ENCAMPMENT_CLASS_IDS = 660,672,797,915): assess whether the narrow 4-class proxy set is likely to systematically under-detect real encampment/bedding/shelter scenes not captured by 'mobile_home/tent/sleeping_bag/yurt' (e.g. a shopping cart with belongings, a cardboard structure, a doorway sleeping setup) in a way that matters given fail-closed is supposed to catch uncertainty — a narrow proxy with high confidence in the WRONG direction (confidently clear on a real encampment scene) defeats the fail-closed design entirely",
    "encampment_gate.py / encampment_fixtures.py scope-note honesty: confirm the gate's self-reported claims (proves drop LOGIC not model ACCURACY) are accurate given what the code actually asserts, and that check_encampment_drop.py's release-blocking wiring in preflight.sh can't be silently skipped or downgraded to advisory in a way inconsistent with its release-blocking claim"
  ],
  "severity_examples": [
    "P0: a code path in extract.py where a frame is processed for garment/attribute extraction BEFORE or WITHOUT the encampment classifier having run on it (e.g. an early-return, a batch-processing shortcut, or a config path that disables the classifier check) — this would let an encampment frame produce ordinary records, violating the exclusion completely",
    "P0: the classifier erroring/timing out (ONNX load failure, malformed frame, exception in classify()) causes the frame to be treated as confidently-clear (extracted) rather than dropped — inverts the fail-closed contract on the exact failure mode fail-closed exists to handle",
    "P1: DropPolicy allows construction with `drop_at_x100 <= certain_clear_below_x100` (or negative/out-of-range values) with no validation, silently making `should_drop` always-true or creating a confusing threshold inversion if any caller ever constructs DropPolicy with non-default values",
    "P1: the 4-class ImageNet proxy scoring confidently LOW (clear) on a real encampment scene it wasn't trained to recognize (e.g. bedding without a visible tent silhouette) — the honest scope note covers 'accuracy unproven' but does it cover the more dangerous case of confidently-wrong-in-the-unsafe-direction, and does anything in the design bias the proxy toward high recall the way DropPolicy's own thresholds do",
    "P2: frames_dropped_encampment counter increments happening in a code path that could double-count or under-count relative to actual frames processed, skewing the coverage-vs-demographics honesty accounting"
  ],
  "success_hints": [
    "Hand-trace all three DropPolicy bands (confident-clear / uncertain / positive) against the single `>=` comparison in should_drop and confirm the two-constant design collapses correctly to fail-closed only because of the DEFAULT ordering (50 > 20), then check whether that ordering is enforced anywhere or just assumed",
    "Read extract.py's full frame-processing order (not just the docstring) to find the exact line where encampment classification gates the rest of the pipeline, and check every branch (including error/except blocks) for what happens on a non-confident-clear or classifier-failure outcome",
    "Compare the ImageNet class indices (660, 672, 797, 915) against the actual ILSVRC2012 label list independently rather than trusting the comment's claim that they were 'verified' — confirm the softmax-mass approach can't be dominated by a single unrelated high-confidence class in a way that suppresses the encampment signal",
    "Check whether any test or fixture exercises the classifier-failure / model-missing path specifically for the fail-closed direction (as opposed to just the fake-scripted-score happy path)"
  ],
  "task_context": "Find any path where this PR's changes let raw footage, an unredacted face/plate, or an encampment observation cross the trust boundary or enter a corpus/record — and any place a fail-closed check (redaction recall, encampment drop, receipt verification, k-anon composition) can silently open, default, or be bypassed. Verify Swift↔Python byte-identity for receipts. Rank by privacy-boundary risk, not style. Scope for this agent: the §8 encampment drop-class classifier, DropPolicy, extraction-path wiring, and the fixtures/gate in jawnscope-node/src/jawnscope_pipeline/encampment.py, encampment_fixtures.py, encampment_gate.py, extract.py's encampment integration, and scripts/check_encampment_drop.py."
}
