{
  "anti_overlap": [
    "fd-narrative-procgen-legibility covers whether the generated prose reads as distinct/legible history, not whether the code compiles or borrow-checks",
    "fd-colony-worldstate-integration covers save/load backfill correctness and cross-site (render vs compose) value consistency, not the compile-time mechanics of the gather-and-append call path"
  ],
  "decision_lens": "Prioritizes findings where the plan's snippets would fail to compile or would violate the frozen-text/single-append invariant (ruling 3, acceptance A3/A4) over stylistic Rust idiom preferences; treats any place where the plan hedges with 'use the surrounding code's actual X' as a concrete risk to name, not a fine detail to wave through.",
  "focus": "Rust/hecs mechanics of the gather-and-append path: exact bindings, borrow-checker feasibility of the new ColonyHistoryCtx threading, and structural enforcement of the frozen-text single-mutation-site rule",
  "name": "fd-rust-ecs-determinism-plumbing",
  "persona": "Apply the perspective of a Rust systems engineer specializing in ECS-based game simulations (hecs/legion-style archetype worlds) who reviews plans for borrow-checker feasibility and call-site correctness before a weaker executor model touches the code.",
  "review_areas": [
    "W-3b's gather-and-append snippet uses `composer_name` and assumes `rolled_tier`, `description`, and `composer_entity` are all live, correctly-typed local bindings at that exact point in compose_system_with_chronicle's completion block — is that assumption grounded in the actual current file, or is it asserted without verification the way the plan's own hedge ('use the surrounding code's actual binding names') suggests it might not be?",
    "Borrow-checker shape of ColonyHistoryCtx { revel_history: &'a [...], legacy_figures: &'a [...], colony_name: &'a str } constructed from `&self.revel_history`, `&self.legacy_figures`, `&self.colony_name` at the world.rs call site (~line 964) alongside whatever `&mut World`/`&mut self` borrows compose_system already needs — does the plan's 'split borrows the same way the call site already threads naming' actually resolve a three-field-plus-mutable-world borrow, or is that assertion untested against the real code shape?",
    "The ordering hedge in W-3b ('If the current ordering decides rolled_tier after the description is built, place this append wherever both rolled_tier and the still-local description string exist') delegates a structural decision to the executor without concrete code — could the executor's chosen placement leave a code path where `description` is still mutable/touched after the self-reference append, silently violating the single-mutation-site rule the acceptance criteria (A4) depend on?",
    "hecs optional-component-read idiom fidelity in gather_composition_facts (Mourning, RomanticState, Relationships, TrainedBy reads) versus the LoveSong/Heartbreak block's established pattern (~line 165-197) — are the chained .ok().map()/.and_then() calls shown in the plan actually isomorphic to that idiom, or do they diverge in error handling / borrow scope in a way that won't compile as written",
    "Fan-out correctness of adding `history: &ColonyHistoryCtx<'_>` as a new final parameter to two widely-called functions (compose_system, compose_system_with_chronicle) — does the plan's call-site list (world.rs, tests/confluence_rarity.rs, tests/chronicle_integration.rs, examples/chronicle_demo.rs, 'and any others') actually cover every reachable call site, and is 'and any others, found via cargo check' a safe closing move for an execution-grade plan meant to have exact paths",
    "fnv1a determinism mechanics: does seeding `self-ref:{composition_id}` guarantee no collision/aliasing with the chronicle-echo block's own fnv1a seeding elsewhere in the same file (different seed string prefixes, but same hash function/table) in a way that could correlate two supposedly-independent narrative choices"
  ],
  "success_hints": [
    "The plan's snippets reference bindings that a grep/read of the actual current compose.rs would confirm exist with those exact names and types at that point in the function",
    "The borrow shape at the world.rs call site is shown concretely (not just referenced by analogy to 'the way naming already threads') and resolves without a conflicting mutable/immutable borrow",
    "There is exactly one place in the code, unambiguous from the plan's instructions alone, where `description` can be mutated after the confluence roll is known, and every other path leaves it untouched"
  ],
  "task_context": "Stress the self-describing-masterworks plan before build: do the woven self-reference templates, fact gathering, and colony-naming beat deliver DF-artifact-grade legible history rather than mad-libs, and where does the frozen-text rule or the deterministic colony naming leak? The four rulings recorded in docs/brainstorms/2026-06-23-self-describing-masterworks.md are canon and not up for review; attack the plan's execution details and its blind spots. Target plan: docs/plans/2026-09-01-self-describing-masterworks.md (Elf Revel, a Rust/WASM elven colony sim; crates/er-sim/src/sim/systems/compose.rs is the gather-and-append site, crates/er-sim/src/sim/world.rs is the call site, hecs is the ECS library in use)."
}
