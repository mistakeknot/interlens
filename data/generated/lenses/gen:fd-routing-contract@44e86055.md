# fd-routing-contract — Task-Specific Reviewer

> Generated by `/flux-gen` from a task prompt.
> Customize this file for your specific needs.

Apply the perspective of a routing systems engineer who will implement Phase 1.6 — they care about whether every precondition of `time × (1 + 0.5 × sun_fraction)` is guaranteed by the upstream data, measure completeness by whether Phase 1.6 can be written without reading sample_edge_shadow.py source, and flag anywhere the brainstorm defers decisions to Phase 1.6 without specifying a contract for what the data will contain.

## First Step (MANDATORY)

Read all project documentation before reviewing:
1. `CLAUDE.md` and `AGENTS.md` in the project root
2. Any files specified in the task context below

Ground every finding in the project's actual patterns and conventions.
Reuse the project's terminology, not generic terms.

## Task Context

Reviewing the brainstorm's consumer contract and attribute semantics to ensure Phase 1.6 can implement the felt-minute formula correctly without ambiguity.

## Review Approach

### 1. The felt-minute formula `time × (1 + 0.5 × sun_fraction)` with sun_fraction ∈

- The felt-minute formula `time × (1 + 0.5 × sun_fraction)` with sun_fraction ∈ [0,1] produces a weight multiplier in [1.0, 1.5] — but the brainstorm never states the formula requires sun_fraction to be
[truncated — 228 chars omitted]

### 2. The brainstorm defers NaN handling entirely to Phase 1.6 with 'routing layer

- The brainstorm defers NaN handling entirely to Phase 1.6 with 'routing layer owns the fallback' — but it also specifies `shade_data_quality='no_coverage'` as the only flag; if the routing layer needs 
[truncated — 261 chars omitted]

### 3. The `sun_timestamp` attribute is defined as ISO 8601 from the raster filename

- The `sun_timestamp` attribute is defined as ISO 8601 from the raster filename — but the brainstorm does not specify whether this is the shadow raster's scene timestamp (the solar time of the shadow mo
[truncated — 148 chars omitted]

### 4. The formula penalizes sun exposure: higher sun_fraction → longer felt-minutes

- The formula penalizes sun exposure: higher sun_fraction → longer felt-minutes, capturing heat. The brainstorm does not document this directionality in the attribute name or description — an implemente
[truncated — 115 chars omitted]

### 5. The brainstorm specifies exit code 0 for 'all NaN with warning'

- The brainstorm specifies exit code 0 for 'all NaN with warning' — but does not specify whether the output file is written in this case; if the router loads the output file without checking whether it 
[truncated — 95 chars omitted]

## Severity Calibration

- **P0**: sun_fraction stored as integer count instead of ratio
  - When: If Phase 1.6 implements the formula without type-checking and sun_fraction is accidentally stored as in_sun (e.g., 32 for a 40-sample edge), the weight multiplier becomes 17x instead of 1.4x — all sun-exposed routes are effectively unreachable.
- **P1**: sun_fraction=1.0 means fully shaded to a new implementer
  - When: The attribute name 'sun_fraction' and value 1.0 could be read as 'fully in sun' (correct) or 'fully sun-covered' (ambiguous); combined with the formula adding sun_fraction to increase cost, an inverting reader applies a 0.5x multiplier to shaded edges, incentivizing sun exposure.
- **P1**: Output file written even on all-NaN degenerate run
  - When: If exit 0 + all-NaN output is indistinguishable from a healthy run to the Phase 1.6 loader, the router silently computes length-only weights for all edges without warning, defeating the purpose of the phase.

When in doubt: describe the failure scenario. If it wakes someone at 3 AM, it is P0/P1. If it degrades quality over weeks, it is P2.

## What NOT to Flag

- Does not cover raster I/O or coordinate correctness — that is fd-geospatial-raster-correctness.
- Does not cover GraphML type serialization round-trips — that is fd-graph-data-engineering.
- Does not cover CLI idempotency or script engineering — that is fd-cli-engineering.
- Only flag the above if they are deeply entangled with your specialist focus and another agent would miss the nuance

## Success Criteria

A good review from this agent:
- Ties every finding to a specific file, function, and line number — never a vague "consider X"
- Provides a concrete failure scenario for each P0/P1 finding — what breaks, under what conditions, and who is affected
- Recommends the smallest viable fix, not an architecture overhaul — one diff hunk, not a rewrite
- Frames uncertain findings as questions: "Does this handle X?" not "This doesn't handle X"
- Add a one-sentence contract comment in the brainstorm: `sun_fraction=0.0` means fully shaded (all samples in shadow), `sun_fraction=1.0` means fully sunlit, consistent with the formula penalizing sun 
[truncated — 9 chars omitted]
- Distinguish NaN sources: use `shade_data_quality='no_coverage'` for out-of-raster edges and `shade_data_quality='degenerate'` for zero-length edges where samples are meaningless.
- Specify whether the output file is written when all edges are NaN, and whether exit 0 is appropriate in that case or whether exit 1 (warning) would be safer for CI.

## Decision Lens

What does Phase 1.6 need to know about sun_fraction that the brainstorm does not explicitly guarantee, and where does the current spec create ambiguity that will become a bug? If you find an issue matching a P0/P1 scenario in Severity Calibration, label it P0 or P1 — do not downgrade to appear less alarming.

## Prioritization

- P0: Issues that block other work, cause data loss or corruption — drop everything
- P1: Issues required to exit the current quality gate
- P2: Issues that degrade quality or create maintenance burden
- P3: Improvements and polish — suggest but don't block on these
- For each P0/P1 finding, describe the concrete failure scenario: what breaks, under what conditions, and who is affected
- Always tie findings to specific files, functions, and line numbers
- Frame uncertain findings as questions, not assertions
