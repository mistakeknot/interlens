name: Pre-compute Embeddings

# Run when new lenses are added to Supabase
# Trigger manually or via webhook from lens ingestion system

on:
  workflow_dispatch:  # Manual trigger
    inputs:
      limit:
        description: 'Limit number of lenses (leave empty for all)'
        required: false
        type: string

  # Also run on schedule to catch any missed lenses
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday at midnight

jobs:
  precompute:
    runs-on: ubuntu-latest

    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@5ef0c079ce82195b2a36a210272d6b661572d83e # v2.14.2
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Pre-compute embeddings
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          if [ -n "${{ inputs.limit }}" ]; then
            python scripts/reembed_with_sentence_transformers.py --limit ${{ inputs.limit }}
          else
            python scripts/reembed_with_sentence_transformers.py
          fi

      - name: Notify on success
        if: success()
        run: |
          echo "✅ Embeddings pre-computed successfully"
          echo "🎉 Search now works without OpenAI API costs!"

      - name: Notify on failure
        if: failure()
        run: |
          echo "❌ Embedding pre-computation failed"
          echo "Check the logs for errors"
