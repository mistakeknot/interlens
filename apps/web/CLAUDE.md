# Interlens Web

> See `AGENTS.md` for architecture, features, deployment, and troubleshooting.

React SPA for exploring FLUX analytical lenses. Live at https://interlens.com.

## Quick Commands

```bash
npm install     # Install deps
npm start       # Dev server at http://localhost:3000
npm run build   # Production build
npm test        # Run tests
```
